/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./app/**/*.{ts,tsx}", // Scans everything under the `app` directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
